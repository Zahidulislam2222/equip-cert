# GENERATED by scripts/gen-deploy-artifacts.mjs from deploy/deploy.config.json — do not hand-edit.
# Ingress only from the ingress controller namespace, only on the application port.
apiVersion: "networking.k8s.io/v1"
kind: "NetworkPolicy"
metadata:
  name: "equipcert"
  namespace: "equipcert"
  labels:
    app.kubernetes.io/name: "equipcert"
    app.kubernetes.io/component: "web"
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: "equipcert"
      app.kubernetes.io/component: "web"
  policyTypes:
    - "Ingress"
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: "ingress-nginx"
      ports:
        - protocol: "TCP"
          port: 8080
