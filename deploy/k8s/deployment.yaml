# GENERATED by scripts/gen-deploy-artifacts.mjs from deploy/deploy.config.json — do not hand-edit.
# Deployment: rolling update at full capacity, readiness drain, restricted pod security.
apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "equipcert"
  namespace: "equipcert"
  labels:
    app.kubernetes.io/name: "equipcert"
    app.kubernetes.io/component: "web"
spec:
  replicas: 3
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app.kubernetes.io/name: "equipcert"
      app.kubernetes.io/component: "web"
  strategy:
    type: "RollingUpdate"
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: "25%"
  template:
    metadata:
      labels:
        app.kubernetes.io/name: "equipcert"
        app.kubernetes.io/component: "web"
    spec:
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: "RuntimeDefault"
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: "topology.kubernetes.io/zone"
          whenUnsatisfiable: "ScheduleAnyway"
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: "equipcert"
              app.kubernetes.io/component: "web"
        - maxSkew: 1
          topologyKey: "kubernetes.io/hostname"
          whenUnsatisfiable: "ScheduleAnyway"
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: "equipcert"
              app.kubernetes.io/component: "web"
      containers:
        - name: "web"
          image: "ghcr.io/zahidulislam2222/equipcert:release"
          imagePullPolicy: "Always"
          ports:
            - name: "http"
              containerPort: 8080
          env:
            - name: "SELF_HOST_PORT"
              value: "8080"
            - name: "SELF_HOST_DRAIN_DELAY_MS"
              value: "5000"
            - name: "SELF_HOST_SHUTDOWN_GRACE_MS"
              value: "20000"
            - name: "SELF_HOST_KEEPALIVE_TIMEOUT_MS"
              value: "65000"
            - name: "SELF_HOST_REQUEST_TIMEOUT_MS"
              value: "120000"
          envFrom:
            - configMapRef:
                name: "equipcert-config"
            - secretRef:
                name: "equipcert-secrets"
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1"
              memory: "512Mi"
          startupProbe:
            httpGet:
              path: "/healthz"
              port: "http"
            periodSeconds: 1
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: "/readyz"
              port: "http"
            periodSeconds: 2
            timeoutSeconds: 1
            failureThreshold: 1
          livenessProbe:
            httpGet:
              path: "/healthz"
              port: "http"
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - "ALL"
          volumeMounts:
            - name: "tmp"
              mountPath: "/tmp"
      volumes:
        - name: "tmp"
          emptyDir:
            medium: "Memory"
            sizeLimit: "64Mi"
